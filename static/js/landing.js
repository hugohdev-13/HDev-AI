"use strict";const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;function initNavbarScroll(){const nav=document.querySelector(".public-nav");if(nav)addEventListener("scroll",()=>nav.classList.toggle("navbar-scrolled",scrollY>12),{passive:true})}function initMobileMenu(){const b=document.querySelector(".public-nav__toggle"),m=document.querySelector(".public-nav__links");if(b&&m)b.addEventListener("click",()=>{const open=m.classList.toggle("is-open");b.setAttribute("aria-expanded",String(open))})}function initMetricCounters(){if(reduced)return;document.querySelectorAll("[data-count]").forEach(e=>{const n=Number(e.dataset.count)||0;e.textContent="0";new IntersectionObserver(x=>x[0].isIntersecting&&(e.textContent=n),{threshold:.5}).observe(e)})}function initScrollReveal(){}function initCardTilt(){}function initHeroParallax(){}document.addEventListener("DOMContentLoaded",()=>{initNavbarScroll();initMobileMenu();initMetricCounters();initScrollReveal();initCardTilt();initHeroParallax()});
