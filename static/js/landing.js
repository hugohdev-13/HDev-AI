"use strict";const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;function initNavbarScroll(){const nav=document.querySelector(".public-nav");if(nav)addEventListener("scroll",()=>nav.classList.toggle("navbar-scrolled",scrollY>12),{passive:true})}function initMobileMenu(){const b=document.querySelector(".public-nav__toggle"),m=document.querySelector(".public-nav__links"),backdrop=document.querySelector(".mobile-backdrop"),close=document.querySelector(".mobile-close");if(!b||!m)return;const set=open=>{m.classList.toggle("is-open",open);backdrop?.classList.toggle("is-open",open);b.setAttribute("aria-expanded",String(open));document.body.style.overflow=open?"hidden":""};b.addEventListener("click",()=>set(!m.classList.contains("is-open")));close?.addEventListener("click",()=>set(false));backdrop?.addEventListener("click",()=>set(false));m.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>set(false)));addEventListener("keydown",e=>e.key==="Escape"&&set(false))}function initMetricCounters(){if(reduced)return;document.querySelectorAll("[data-count]").forEach(e=>{const n=Number(e.dataset.count)||0;e.textContent="0";new IntersectionObserver(x=>x[0].isIntersecting&&(e.textContent=n),{threshold:.5}).observe(e)})}function initScrollReveal(){}function initCardTilt(){}function initHeroParallax(){}document.addEventListener("DOMContentLoaded",()=>{initNavbarScroll();initMobileMenu();initMetricCounters();initScrollReveal();initCardTilt();initHeroParallax()});
